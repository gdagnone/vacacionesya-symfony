{% extends 'base.html.twig' %}

{% block title %}Tipo de Operaciones{% endblock %}

{% block body %}
    
    
                <h3>Tipo de Operaciones</h3>

                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ tipo_operacione.id }}</td>
                        </tr>
                        <tr>
                            <th>Oper_detalle</th>
                            <td>{{ tipo_operacione.operDetalle }}</td>
                        </tr>
                        <tr>
                            <th>Oper_activo</th>
                            <td>{{ tipo_operacione.operActivo ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>Oper_orden</th>
                            <td>{{ tipo_operacione.operOrden }}</td>
                        </tr>
                    </tbody>
                </table>
                

    <a href="{{ path('tipo_operaciones_index') }}"><i class="fas fa-arrow-circle-left"></i> volver</a>&nbsp;&nbsp;

    <a href="{{ path('tipo_operaciones_edit', {'id': tipo_operacione.id}) }}"><i class="fas fa-edit"></i> editar</a>

    {{ include('tipo_operaciones/_delete_form.html.twig') }}
{% endblock %}
